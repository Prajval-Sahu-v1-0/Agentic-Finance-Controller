# Core
requests>=2.31.0
pydantic>=2.7.0

# Optional: only needed by src/inspect_benchrec.py for JSON/Excel/Parquet/
# SQLite dataset inspection (CSV inspection, which is all BenchRec needs
# today, does not require pandas).
pandas>=2.2.0

# CLI output
rich>=13.7.0

# Demo API + dashboard (src/api.py) — thin REST wrapper around the
# existing pipeline, for the buildathon submission demo
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Testing
pytest>=8.2.0
