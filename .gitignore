# Virtual environment
.venv/
venv/

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/

# Downloaded / derived external datasets (large, reproducible via
# src/inspect_benchrec.py + src/benchrec_map.py — not committed)
data/external/raw/
data/external/processed/

# User-uploaded datasets (src/api.py's POST /upload) — not committed
data/uploaded/

# PyInstaller build output — reproducible from recon-agent.spec, not
# committed (dist/recon-agent.exe alone is ~45MB)
build/
dist/

# OS
.DS_Store
Thumbs.db
